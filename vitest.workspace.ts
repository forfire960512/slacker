// Only the packages that actually have tests so far — extend this array as
// more packages (apps/*) gain test suites (see plan.md's "자동화 테스트
// 스위트" TODO for the phased rollout this follows).
export default ["packages/core", "server", "packages/ui"];
